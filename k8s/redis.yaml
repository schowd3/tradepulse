apiVersion: apps/v1
kind: Deployment
metadata:
  name: tradepulse-redis
  namespace: tradepulse
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tradepulse-redis
  template:
    metadata:
      labels:
        app: tradepulse-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: tradepulse-redis
  namespace: tradepulse
spec:
  selector:
    app: tradepulse-redis
  ports:
    - port: 6379
      targetPort: 6379